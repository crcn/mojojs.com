<ul class="ide-tabs">  

  {{#if: !showPreview }}
    {{ html: sections.tabs }}
  {{/}}


  {{#if:compile}}
    <li class="toggle" data-bind="{{ onClick: togglePreview() }}">
      {{ showPreview ? 'see code' : 'see preview' }}
    </li>
  {{/}}
</ul>
