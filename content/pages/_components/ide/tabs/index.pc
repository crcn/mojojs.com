<ul class="ide-tabs">  

  {{#if: !showPreview }}
    {{ html: sections.tabs }}
  {{/}}

  <li class="toggle" data-bind="{{ onClick: togglePreview() }}">
    {{ showPreview ? 'edit' : 'preview' }}
  </li>
</ul>
