<div class="fast-snippet">
  <a href="#" data-bind="{{ onClick: renderItems() }}">Render {{count}} items {{ duration ? "(" + duration + " ms)" : undefined }}</a>

  <ul>
    {{ html: children.items }}
  </ul>
</div>