// Generated by CoffeeScript 1.6.2
(function() {
  var Ewe, Group, services;

  Group = require("./group");

  services = require("./services");

  Ewe = (function() {
    /*
    */
    function Ewe(options) {
      if (options == null) {
        options = {};
      }
      this.service = new services[options.service || "optimizely"](options);
      this._groups = {};
    }

    /*
     scope identifier used to clasify exactly what variants to use. Note 
     that many users might have the same scope - depending on what country
     they might live in, or other variables. Scope can also be specific to a user
     ID
    */


    Ewe.prototype.group = function(key) {
      return this._groups[key] || (this._groups[key] = new Group(key, this));
    };

    return Ewe;

  })();

  module.exports = function() {
    return new Ewe();
  };

}).call(this);
