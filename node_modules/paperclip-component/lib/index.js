// Generated by CoffeeScript 1.7.1
(function() {
  var SectionBlockBinding, componentBlockBinding, registerComponents;

  componentBlockBinding = require("./componentBlockBinding");

  SectionBlockBinding = require("./sectionBlockBinding");


  /*
   */

  registerComponents = function(components, pc) {
    var name, o, register;
    register = function(key, value) {
      return pc.blockBinding(key, componentBlockBinding(value));
    };
    for (name in (o = components.toJSON())) {
      register(name, o[name]);
    }
    return components.on("change", register);
  };


  /*
   */

  module.exports = function(components) {
    return function(pc) {
      registerComponents(components, pc);
      return pc.blockBinding("section", SectionBlockBinding);
    };
  };

}).call(this);
